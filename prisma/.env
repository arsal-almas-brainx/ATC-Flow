# Local database location. Relative sqlite paths resolve next to schema.prisma,
# so this is prisma/dev.sqlite.
#
# Kept in prisma/.env rather than the project-root .env because the Shopify CLI
# manages that file. In production the host sets a real DATABASE_URL env var,
# which takes precedence over this file.
DATABASE_URL="file:dev.sqlite"
